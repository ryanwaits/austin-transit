export function Transition() {
  return (
    <section className="col col-narrow prose" style={{ paddingBlock: "clamp(56px, 11vh, 130px)" }}>
      <p style={{ fontSize: "var(--text-h5)", lineHeight: 1.4 }}>
        But a single network number is an average, and an average is a place to hide. It blends the
        route that runs every twelve minutes through downtown with the route that comes twice an
        hour on the eastern edge of the county. The people waiting on those two routes are not the
        same people, and they are not waiting the same amount.
      </p>
    </section>
  );
}
